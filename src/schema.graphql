type Query {
    dummy: Void
}

type Mutation {
    markAsRecommended(
        id: ID!
        marked: Boolean!
    ): Void
    setDisplayState(
        id: ID!
        displayed: Boolean!
    ): Void
}

enum Void {
    VOID
}